{
    "name": "browser-storage-plus",
    "version": "0.0.1",
    "description": "Enhanced wrapper for localStorage/sessionStorage with additional features like type safety, expiration, encryption, and events",
    "scripts": {
        "build": "rm -rf dist && tsc"
    },
    "devDependencies": {
        "typescript": "^5.5.4"
    },
    "dependencies": {
        "js-sha256": "^0.11.0"
    }
}
